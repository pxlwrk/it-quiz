<form method="post" action="{{ path('app_topic_delete', {'id': topic.id}) }}"
      onsubmit="return confirm('Wirklich das Thema löschen?');"
      class="mt-3 m-auto text-end"
>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ topic.id) }}">
    <button class="btn btn-danger btn-sm">L&ouml;schen</button>
</form>
