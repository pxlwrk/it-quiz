<form method="post" action="{{ path('app_question_delete', {'id': question.id}) }}"
      onsubmit="return confirm('Wirklich die Frage löschen?');"
      class="mt-3 m-auto text-end"
>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question.id) }}">
    <button class="btn btn-danger btn-sm">L&ouml;schen</button>

</form>
