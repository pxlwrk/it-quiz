<form method="post" action="{{ path('app_event_session_delete', {'id': event_session.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');"
      class="mt-3 m-auto text-end"
>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_session.id) }}">
    <button class="btn btn-danger btn-sm">L&ouml;schen</button>
</form>
