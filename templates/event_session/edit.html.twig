{% extends 'base.html.twig' %}

{% block title %}Edit EventSession{% endblock %}

{% block body %}
    <h1>Edit EventSession</h1>

    {{ include('event_session/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_event_session_index') }}">back to list</a>

    {{ include('event_session/_delete_form.html.twig') }}
{% endblock %}
